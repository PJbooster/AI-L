{
  "name": "ai_app_monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm run dev --prefix front",
    "dev:backend": "cd back && .venv\\Scripts\\python -m uvicorn app.main:app --reload --port 5000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
